import { createServiceRole } from '@/lib/supabaseServer'; export const runtime='nodejs'; export const dynamic = 'force-dynamic'; export default async function Users(){ const s=createServiceRole(); const { data } = await s.from('profiles').select('id,email,role,created_at'); return (<div className='p-6'><h2 className='text-xl'>Users</h2><table className='w-full'><thead><tr><th>ID</th><th>Email</th><th>Role</th></tr></thead><tbody>{data?.map((u:any)=>(<tr key={u.id}><td>{u.id}</td><td>{u.email}</td><td>{u.role}</td></tr>))}</tbody></table></div>);}
