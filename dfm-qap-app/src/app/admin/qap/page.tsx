import { createServiceRole } from '@/lib/supabaseServer'; export const runtime='nodejs'; export const dynamic = 'force-dynamic'; export default async function QAP(){ const s=createServiceRole(); const { data } = await s.from('qap').select('*').order('created_at',{ascending:false}); return (<div className='p-6'><h2 className='text-xl'>QAPs</h2><table className='w-full'><thead><tr><th>ID</th><th>Part</th><th>PDF</th></tr></thead><tbody>{data?.map((q:any)=>(<tr key={q.id}><td>{q.id}</td><td>{q.part_id}</td><td>{q.pdf_key}</td></tr>))}</tbody></table></div>);}
