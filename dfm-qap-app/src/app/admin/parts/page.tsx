import { createServiceRole } from '@/lib/supabaseServer'; export const runtime='nodejs'; export const dynamic = 'force-dynamic'; export default async function Parts(){ const s=createServiceRole(); const { data } = await s.from('parts').select('*').order('created_at',{ascending:false}); return (<div className='p-6'><h2 className='text-xl'>Parts</h2><table className='w-full'><thead><tr><th>ID</th><th>User</th><th>Name</th><th>Material</th></tr></thead><tbody>{data?.map((p:any)=>(<tr key={p.id}><td>{p.id}</td><td>{p.user_id}</td><td>{p.name}</td><td>{p.material}</td></tr>))}</tbody></table></div>);}
