import { createServiceRole } from '@/lib/supabaseServer'; export const runtime='nodejs'; export const dynamic = 'force-dynamic'; export default async function Jobs(){ const s=createServiceRole(); const { data } = await s.from('analysis_jobs').select('*').order('created_at',{ascending:false}); return (<div className='p-6'><h2 className='text-xl'>Jobs</h2><table className='w-full'><thead><tr><th>ID</th><th>Part</th><th>Status</th></tr></thead><tbody>{data?.map((j:any)=>(<tr key={j.id}><td>{j.id}</td><td>{j.part_id}</td><td>{j.status}</td></tr>))}</tbody></table></div>);}
